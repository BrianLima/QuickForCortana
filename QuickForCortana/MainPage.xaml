<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QuickForCortana"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Models="using:QuickForCortana.Models"
    x:Class="QuickForCortana.MainPage"
    mc:Ignorable="d">

    <SplitView x:Name="MySplitView" DisplayMode="CompactOverlay"  IsPaneOpen="False" 
		CompactPaneLength="50" OpenPaneLength="150">
        <SplitView.Pane>
            <StackPanel Background="Gray">
                <Button x:Name="HamburgerButton" FontFamily="Segoe MDL2 Assets" Content="&#xE700;"
					Width="50" Height="50" Background="Transparent" Click="HamburgerButton_Click"/>
                <StackPanel Orientation="Horizontal">
                    <Button x:Name="MenuButton1" FontFamily="Segoe MDL2 Assets" Content="&#xE10F;"
						Width="50" Height="50" Background="Transparent"/>
                    <TextBlock Text="Home" FontSize="18" VerticalAlignment="Center" />
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Button x:Name="MenuButton2" FontFamily="Segoe MDL2 Assets" Content="&#xE1D6;"
						Width="50" Height="50" Background="Transparent"/>
                    <TextBlock Text="New Note" FontSize="18" VerticalAlignment="Center" />
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Button x:Name="MenuButton3" FontFamily="Segoe MDL2 Assets" Content="&#xE115;"
						Width="50" Height="50" Background="Transparent"/>
                    <TextBlock Text="Settings" FontSize="18" VerticalAlignment="Center" />
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Button x:Name="MenuButton4" FontFamily="Segoe MDL2 Assets" Content="&#xE11B;"
						Width="50" Height="50" Background="Transparent"/>
                    <TextBlock Text="Help" FontSize="18" VerticalAlignment="Center" />
                </StackPanel>
<<<<<<< HEAD

            </StackPanel>
        </SplitView.Pane>
        <StackPanel>
            <ListBox x:Name="listView" HorizontalAlignment="Left" VerticalAlignment="Top" Width="auto" DataContext="{Binding notes}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Models:NoteControl/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
=======
            </StackPanel>
        </SplitView.Pane>
        <SplitView.Content>
            <ListView ItemsSource="{Binding Path=Notes}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="376*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Margin="9,40,139,0" Text="{Binding Path=Date}" VerticalAlignment="Top" Grid.Column="1" Height="27" FontSize="14"/>
                            <TextBlock Margin="9,3,10,0" TextWrapping="Wrap" Text="{Binding Path=Content}" VerticalAlignment="Top" Height="37" FontSize="28" Grid.Column="1"/>
                            <Rectangle Grid.Column="1" Fill="{ThemeResource ComboBoxHeaderForegroundThemeBrush}" Height="2" Margin="4,0,0,3" VerticalAlignment="Bottom" HorizontalAlignment="Left" Width="389" Stroke="{ThemeResource MediaTextThemeBrush}"/>
                            <Button x:Name="button" FontFamily="Segoe MDL2 Assets" Content="&#xE104;" Grid.Column="1" HorizontalAlignment="Right" Margin="0,0,10,15" VerticalAlignment="Bottom" Width="36" Height="27"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </SplitView.Content>
>>>>>>> origin/master
    </SplitView>

</Page>
